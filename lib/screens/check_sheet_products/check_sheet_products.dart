import 'package:flutter/material.dart';

class CheckSheetProducts extends StatelessWidget {
  const CheckSheetProducts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    //create bar code screen check
    return Container();
  }
}

